sever:
  port: 443

  ssl:
    key-store: classpath:keystore.p12
    key-store-type: PKC12
    key-store-password: moyeo1234

spring:
  port: 8080
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/moyeo?serverTimezone=UTC
    username: root
    password: admin1234
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
